# When running locally without Docker — points to FastAPI directly
VITE_API_URL=http://localhost:8000

# When deployed (e.g. Render/Railway) set this to your backend URL
# VITE_API_URL=https://your-backend.onrender.com
